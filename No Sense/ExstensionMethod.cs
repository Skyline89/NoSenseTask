using System;
using System.Collections.Generic;

namespace No_Sense
{
    public static class ArrayExstension
    {

        //public static List<string> ThisDoesntMakeAnySense<T>(this IEnumerable <T> listOfString)
        //{
        //    foreach (var number in listOfStrings)
        //    {
        //        if (number == null)
        //            throw new NullReferenceException("please enter number instead of null");
        //        else(number I)
        //    }
            
        //    return T;
        //}
    }
}